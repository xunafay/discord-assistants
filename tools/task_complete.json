{
    "name": "task_complete",
    "description": "Create a task",
    "parameters": {
        "type": "object",
        "properties": {
            "user_id": {
                "type": "string",
                "description": "User name/id for which to get the tasks"
            },
            "task_id": {
                "type": "string",
                "description": "Task id"
            }
        },
        "required": ["user_id", "task_id"]
    }
}